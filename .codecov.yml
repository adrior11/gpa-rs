ignore:
  - "src/main.rs"
  - "src/prompts.rs"

codecov:
  require_ci_to_pass: yes

coverage:
  status:
    patch:
      default:
        target: auto
        threshold: 0.10%
    project:
      default:
        target: auto
        threshold: 0.10%
